{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --port 3002\"",
    "front": "npm run dev --prefix frontend -- --port 3002",
    "back": "npm run dev --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "ogl": "^1.0.11"
  }
}
