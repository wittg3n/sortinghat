{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --port 3000\"",
    "front": "npm run dev --prefix frontend -- --port 3000",
    "back": "npm run dev --prefix backend",
    "mongo": "cmd /c \"C: && mongod\""
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "cookie-parser": "^1.4.7",
    "jsonwebtoken": "^9.0.2",
    "nanoid": "^5.1.5",
    "ogl": "^1.0.11",
    "passport-jwt": "^4.0.1"
  }
}
